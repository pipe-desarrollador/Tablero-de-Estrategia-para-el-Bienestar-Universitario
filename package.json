{
  "name": "tablero-bienestar-universitario",
  "version": "1.0.0",
  "description": "Tablero de Estrategia para el Bienestar Universitario",
  "main": "start.js",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "build": "echo 'Build step skipped for Railway deployment'",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "migrate": "echo 'No migrations needed for simple server'"
  },
  "keywords": ["bienestar", "universitario", "dashboard", "analytics"],
  "author": "Felipe",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^5.1.0",
    "cors": "^2.8.5"
  }
}